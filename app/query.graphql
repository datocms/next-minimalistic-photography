query Home {
  works: allWorks {
    id
    coverImage {
      responsiveImage(imgixParams: { auto: format, h: 1400 }) {
        src
        srcSet
        base64
        width
        height
      }
    }
  }

  meta: _allWorksMeta { count }
}